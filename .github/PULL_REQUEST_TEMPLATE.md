## Description

<!-- Briefly describe what this PR does -->

## Related issue(s)

<!-- Link to issue(s): e.g. Fixes #123 -->

## Checklist

- [ ] Tests pass locally (`make test` or `pytest tests`)
- [ ] Lint passes (`make lint` or `ruff check src tests && mypy src/app`)
- [ ] Code is formatted (`make fmt` or `ruff format src tests`)
- [ ] README or docs updated if user-facing behavior changed
- [ ] No secrets or credentials in code or commits
